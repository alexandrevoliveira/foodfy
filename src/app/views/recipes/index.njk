{% extends 'layout.njk' %}


{% block content %}

    <div class="bodyhead-recipes">
        <div>Gerenciar receitas</div>
        <div class="bodyhead-filter">

            <div class="filter">
                <form id="form-filter" action="/admin/recipes" method="GET">
                    <input 
                        type="text" 
                        name="filter"
                        value="{{filter}}"
                        placeholder="Buscar uma receita"
                    >
                </form>
            </div>
            <div>
                <button form="form-filter" class="button" type="submit">Buscar</button>
            </div>
            <div>
                <a class="button" href="/admin/recipes/create">Criar Receita</a>
            </div>

        </div>            
    </div>

    <section class="cards">

        {% for recipe in recipes %}
            <div class="card">
                <div class="card-image">
                    <img src="{{recipe.image}}" alt="{{recipe.name}}">            
                </div>
                <div class="card-info">
                    <div class="recipe-title">{{recipe.title}}</div>
                    <div class="recipe-author">por <a href="/admin/chefs/{{recipe.ched_id}}">{{recipe.chef}}</a></div>
                    <div class="recipe-button"><a href="/admin/recipes/{{recipe.id}}">Visualizar</a></div>
                </div>            
            </div>
        {% endfor %}
            
    </section>

{% endblock %}